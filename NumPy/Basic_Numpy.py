'''
1. In Numpy to create array we used->
# np.array([1,2,3])
2. Similar if we want to create 2-D array
# np.array([1,2,3], [4,5,6])-> Error
3. Array parameter need array and in that array we can give n number of array.
# np.array([[1,2,3], [4,5,6]])-> Correct
4. Element Wise Multiplication in Array Python
Aka in list it multiply same things n times.
5. Array Creation From Scretch
    a. Zero's Array->
        #np.zeros(rows, columns)
    b. One's Array-> 
        #np.ones(rows, columns)
    c. Constant Array-> 
        #np.full(rows, columns),k
    d. Random Array-> 
        #np.random.random(rows, columns)
    e. Sequencial Array-> 
        #np.arange(rows, columns, space)
6. Vector & Matrix & Tensor
    a. Vector-> 1-D Array
    b. Matrix-> 2-D Array
    c. Tensor-> More than 2-D Array
7. 1:27 Phase->3 || Old Documentation is pending.
8. Axis in matrix->
    a. axis= 0-> Column Wise/ Verticle
    b. axis= 1-> Row Wise/ Horizontal
'''